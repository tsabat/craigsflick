<p>You've reached the set page fuck shit piss</p>
<ul>
  <% @sets.each do |set| %>
   <li>
     <%= link_to(image_tag(set[:primary_url], :alt => set[:name]), sets_path+"/#{set[:id]}") %>
   </li>
  <% end %>
</ul>
