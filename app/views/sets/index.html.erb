<p><%= link_to "Logout", sessions_path, {:method => :delete, :confirm => "Are you sure?"} %>
<p>You've reached the set page</p>
<ul>
  <% @sets.each do |set| %>
   <li>
     <%= link_to(image_tag(set[:primary_url], :alt => set[:name]), sets_path+"/#{set[:id]}") %>
   </li>
  <% end %>
</ul>
